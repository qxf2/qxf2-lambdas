region: ap-south-1

function_name: qxf2-employee-messages
handler: daily_messages.lambda_handler
description: Daily messages for employees, trainees, interns, etc.
runtime: python3.8
# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:
